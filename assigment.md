## Задание
Доработать систему, созданную в предыдущей лабораторной работе. Необходимо спроектировать и реализовать модуль работы с внешними данными. Модуль должен выполнять следующие функции:
- принимать файлы с входными данными о товарах и способах транспортировки в разных форматах (`JSON`, `XML`, `CSV`);
    - теперь вместо харкода файла мы спрашиваем у пользователя в каком тпе поступит конфигураци и файл откуда ее взять,потом в main вместо store := infrastructure.NewCsvRepository(logger, logisticFile) мы вызовем что-то такое store := infrastructure.NewRepository(logger, filePath, fileType) что-то такое а в инфре будет фабрика и мы получим конкретную стратегию(т.е алгоритм, и может лучше перименовать не Repository а CargoCfgLoader или что-то подобное) для загрузки json/xml/cxv
- если пользователь после формирования перечня грузов в партии и задания финальной точки не определил тип транспорта, то необходимо отдавать в качестве результата список со всеми возможными вариантами (для каждого вида транспорта);
    - поидее проверка на len(transports) == 0 и вообще теперь надо хранить массив транспортов?!(в Order из domain, но мне кажется это не прям хорошая идея) и тут надо подумать насчет того как выполнить этот пункт 
- должна быть возможность экспортировать ответ системы в файл;
    - в конце  после того как заказ готов не просто делать fmt.Println его как сейчас, а сделать объект который будет принимать стратегию по выводу заказа(cli, json/xml/cxv)

*Структурные паттерны*:
- необходимо реализовать следующие варианты сохранения файла:
    - сохранение в разных типах (`JSON`, `CSV`);
    - сохранение с шифрованием данных;
    - сохранение с сжатием (создается `.zip` архив с файлом);
  - должна быть возможность комбинировать варианты сохранения между собой;

### а вот и доп к 3) тут стратегия + декоратор чтобы сделать некий объект который соберет все что нам надо в 1 конфиг и выполнит попорядку(1 - josn/csv 2 - шифрование 3 - сжатие в zip)

*Поведенческие паттерны*:
- добавить возможность фильтрации по некоторым полям;
  - пользователь может задать сортировку по полям ответа (*название транспорта*, *цена*, *скорость*) перед сохранением;
  - сортировки можно комбинировать между собой.

### 1 - еслть функция для сортировки и туда передаем функцию компаратор
### 2 - функция компаратор непосредтсвенно, как будто явная стратегия(различные функции компараторы, но! может и visitor! его так и не понял)
### 3 - декоратор чтоли

## Definition of done
- Система дополнена согласно заданию.
  - Система протестирована на функциональную полноту и логическую корректность.
  - Паттерны проектирования корректно имплементированы.